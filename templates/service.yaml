apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube_config_devopsday.fullname" . }}
  labels:
    {{- include "kube_config_devopsday.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube_config_devopsday.selectorLabels" . | nindent 4 }}
